# 路线图

`xshow circuits` 正在朝一个友好的电子积木电路实验台推进，用来帮助用户学习和演示简单电路。

项目应该保持直观、轻量、即时反馈：用户可以搭出一个电路，看见发生了什么，并从结果里理解电路，而不需要面对完整工程仿真器的复杂度。

## 当前进度

当前重点：**扩展电路示例和引导式实验提示**。

| 方向 | 状态 | 说明 |
| --- | --- | --- |
| 交互式电路实验台 | 已完成 | 电池、开关、灯泡、可变电阻、导线、亮度和回路状态已经可用。 |
| 导线选择和重接 | v0.1 已完成 | 导线可以选中、删除、拖动端点重接，也会吸附到附近端子，并且有更清晰的 hover、拖拽、释放反馈和更易读的正交折线走线。 |
| 电流方向动画 | 已完成 | 闭合回路中的通电导线已经有电流流动动画，后续还可以继续优化表现。 |
| 引导式实验课程模式 | 正在扩展 | 课程内容、提示、视觉目标和实验初始工作区已经放在可编辑数据中，现在可以切换多个实验，课程面板会自动检查简单电路目标。 |
| 自动部署 | 第一版 | 推送到 `main` 后，可以在配置仓库 secrets 后自动构建，并通过 SSH 把 `dist/` 发布到云服务器。 |

## 近期方向

### 更好的导线交互

状态：**v0.1 已完成**

- [x] 可以在工作台上直接选中导线
- [x] 可以拖动导线端点重新连接到其他端子
- [x] 未选中导线时，也可以直接拖动端点开始重接
- [x] 可以从端子直接拖到端子创建导线，不再需要单独的连线模式
- [x] 按住 Alt 拖动已有导线端点，可以从同一个端子创建分支导线
- [x] 选中的导线始终位于重叠端点之上
- [x] 更清晰地把导线端点吸附到附近端子
- [x] 优化导线路径，让连接关系更容易读懂
- [x] 补充更清晰的 hover、拖拽、释放状态

### 继续优化电流方向动画

状态：**v0.1 已完成，后续继续打磨**

- [x] 当电路闭合时，在导线上显示电流流动动画
- [x] 当开关断开时，电流动画立即停止
- [x] 动画速度可以反映模拟电流强弱
- [ ] 当电池极性变化时，动画方向也随之变化
- [ ] 让灯泡亮度、电流数值和导线动画之间更有视觉关联

### 引导式实验课程模式

状态：**正在扩展**

- [x] 增加一个课程面板，展示分步任务
- [x] 先从电池、开关、灯泡、可变电阻的基础实验开始
- [x] 可以在多个引导式实验之间切换
- [x] 支持简单的成功判断，比如“灯泡已经点亮”
- [x] 课程内容应放在可编辑的数据文件中，避免写死在组件里
- [x] 为每个实验增加一键加载初始工作区
- [x] 为当前未完成步骤增加课程提示
- [x] 为当前步骤在工作台上增加更直观的视觉引导
- [ ] 当一个实验完成时，增加更明确的完成反馈

### 更多电路示例

状态：**下一步**

- [ ] 增加串联电路示例
- [ ] 增加并联电路示例
- [ ] 增加用于比较不同示例亮度的课程检查
- [ ] 电路示例也应放在可编辑数据中

### 自动部署

状态：**第一版已完成**

- [x] 推送到 `main` 时自动构建
- [x] 配置 secrets 后，通过 SSH 把静态 `dist/` 产物发布到云服务器
- [x] 补充服务器和仓库 secrets 配置文档
- [ ] 第一台生产服务器配置完成后，补充公开演示地址

## 后续方向

- [ ] 更多元器件：LED、电机、蜂鸣器、二极管、电容、电流表、电压表
- [ ] 保存和加载电路工作区
- [ ] 将电路导出为图片
- [ ] 通过 URL 分享电路状态
- [ ] 在不变成完整 SPICE 仿真器的前提下，让仿真规则更真实

## 产品原则

- 优先使用直接操作，而不是表单配置
- 第一屏应该直接可用，而不是大段解释
- 通过动画、亮度和状态面板让电路状态可见
- 先使用适合教学的近似模型，再逐步增加物理真实度
- 保持代码规模适中，让学习者也能读懂
