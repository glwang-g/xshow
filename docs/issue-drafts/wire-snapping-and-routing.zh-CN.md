# 优化导线吸附和走线

## 问题

导线已经可以交互编辑，但连接体验还比较粗糙。用户拖动端点时，应该能清楚知道导线将会连接到哪个端子。

## 方案

优化导线端点吸附和走线表现，让电路连接更像真实插线，也更容易阅读。

## 验收标准

- 拖动导线端点时，端点会视觉吸附到附近端子
- 当前可释放的目标端子有明确高亮
- 如果释放在空白区域，取消重接并保留原连接
- 选中的导线始终位于重叠导线和端点之上
- 在可行范围内，导线走线尽量避免遮住元器件文字

## 备注

这里应该保持轻量，不需要一开始就引入完整的图布局引擎。
