# 版本待完成清单

这份清单把当前路线图拆成可发布版本，帮助 `xshow circuits` 从“继续加功能”切换到“收口、验证、发布”。

## v0.2：移动端和分享版

状态：**已作为 v0.2.0 发布**。

v0.2 的核心功能已经基本完成：移动端 HUD、触控平移缩放、课程任务、串并联、LED、PWA、JSON 存档、URL 分享、图片导出都已经在主线中。

### 必须完成

- [x] 基于项目 owner 确认，将 v0.2 接受为可发布版本。
- [ ] 后续按 [移动端 QA 清单](mobile-qa.zh-CN.md) 跑一轮真机回归测试。
- [ ] 记录 iPad 横屏、手机竖屏、手机横屏的测试结果。
- [ ] 修复真机 QA 中发现的阻塞问题。
- [x] 更新 README 截图或补充移动端截图。
- [ ] 部署后确认线上 PWA manifest、service worker、分享链接都可用。
- [x] 写一份 release note。
- [x] 发布接受后给 v0.2.0 打 tag。

### 可以顺手做

- [x] 给移动端任务浮条增加更短的文案，避免遮挡。
- [x] 给分享链接失败、剪贴板失败补更清晰提示。
- [x] 导出 PNG 中补充当前实验标题。
- [x] PWA 更新提示文案再口语化一点。

### 暂不塞进 v0.2

- 云端账号和自动同步。
- 更多元器件。
- 完整物理求解模型。
- 课堂/班级空间。

## v0.3：云端记录和跨设备继续

状态：**功能已完成，等待线上登录 QA**。

参考：[云端同步计划](cloud-sync-plan.zh-CN.md)。Supabase 建表和 RLS 可使用 [supabase-schema.sql](supabase-schema.sql)。

### 必须完成

- [x] 选择 Supabase 作为后端方案。
- [x] 增加邮箱密码登录，但不阻塞免登录使用。
- [x] 增加云端工作台记录列表。
- [x] 支持保存当前工作台到云端。
- [x] 支持从云端加载记录。
- [x] 增加同步状态：未登录、本地修改、同步中、已同步、同步失败。
- [x] 增加冲突处理：覆盖云端或另存为副本。

### 可以顺手做

- [x] 分享链接支持“复制到我的记录”。
- [x] 云端记录支持重命名和删除。
- [x] 首次登录后提示是否上传当前本地工作台。

## v0.4：更多元器件和更真实规则

状态：**第一版已实现，等待回归 QA**。

### 候选元器件

- [x] 蜂鸣器
- [x] 电机
- [x] 二极管
- [x] 电容
- [x] 电流表
- [x] 电压表

### 仿真改进

- [x] 电流方向和电池极性完全联动。
- [x] 灯泡、LED、导线动画和电流数值有更一致的视觉反馈。
- [x] 从单一路径近似升级到更明确的节点/支路模型。
- [x] 增加节点/支路求解器和实体搭建清单的回归测试。

### 实体搭建桥接

- [x] 根据当前工作台生成第一版物料清单。
- [x] 在真实 SKU 映射前，先为每类虚拟元件补充采购关键词。
- [x] 根据当前导线生成接线步骤。
- [x] 增加极性元件和电表接法的实体搭建注意事项。
- [x] 支持将第一版实体搭建方案导出为 Markdown。
- [ ] 将虚拟元件映射到真实套件 SKU 或推荐购买链接。
- [ ] 导出可打印的实体装配单，包含布局和接线说明。

## 发布判断

v0.2 现在只接受阻塞 bug 修复、真机 QA 记录、截图和发布说明更新。新功能进入 v0.3 或 v0.4，避免移动端版本永远差最后一步。
