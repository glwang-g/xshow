# v0.2 QA Report

This document tracks verification status before the v0.2 release.

## Automated Checks

| Item | Status | Notes |
| --- | --- | --- |
| TypeScript / Vite build | Passed | `pnpm build` |
| PWA build output | Passed locally | `dist/` includes `manifest.webmanifest`, `sw.js`, and `offline.html`; production access still needs confirmation. |
| Mobile viewport screenshot | Passed locally | Captured `docs/mobile-screenshot.png` at 390 x 844 with the compact lesson strip visible. |
| URL sharing | Pending production check | Verify copied links restore the workbench after deployment. |

## Real-Device Checks

| Device / Scenario | Status | Notes |
| --- | --- | --- |
| iPad landscape | Pending | Run `docs/mobile-qa.md`. |
| Phone portrait | Pending | Run `docs/mobile-qa.md`. |
| Phone landscape | Pending | Run `docs/mobile-qa.md`. |
| Desktop browser | Pending | Confirm the three-column layout and mouse dragging did not regress. |

## Release Judgment

The current code is ready as a v0.2 release candidate. Before tagging, complete real-device checks and fix any blocking issues found there.
