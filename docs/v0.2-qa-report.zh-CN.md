# v0.2 QA 记录

本文档记录 v0.2 发布前验证状态。

## 自动检查

| 项目 | 状态 | 说明 |
| --- | --- | --- |
| TypeScript / Vite 构建 | 通过 | `pnpm build` |
| PWA 构建产物 | 本地通过 | `dist/` 已包含 `manifest.webmanifest`、`sw.js`、`offline.html`；线上访问仍需确认。 |
| 移动端视口截图 | 本地通过 | 已生成 390 x 844 的 `docs/mobile-screenshot.png`，紧凑任务提示浮条可见。 |
| URL 分享 | 待线上确认 | 需要部署后验证复制链接和打开链接恢复工作台。 |

## 真实设备检查

| 设备/场景 | 状态 | 备注 |
| --- | --- | --- |
| iPad 横屏 | 待测 | 按 `docs/mobile-qa.zh-CN.md` 执行。 |
| 手机竖屏 | 待测 | 按 `docs/mobile-qa.zh-CN.md` 执行。 |
| 手机横屏 | 待测 | 按 `docs/mobile-qa.zh-CN.md` 执行。 |
| 桌面浏览器 | 待测 | 确认三栏布局和鼠标拖拽没有回退。 |

## 发布判断

当前代码已经进入 v0.2 release candidate。打 tag 前，至少需要完成真实设备检查，并修复发现的阻塞问题。
